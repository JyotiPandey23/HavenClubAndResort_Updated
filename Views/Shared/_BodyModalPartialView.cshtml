<!-- Modal -->
<div class="modal fade" id="inquiryModal" tabindex="-1" aria-labelledby="inquiryModalLabel" aria-hidden="true" data-bs-backdrop="static">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content custom-modal">
            <div class="modal-body p-0 custom-modal-body">

                <!-- Custom Close Button -->
                <button class="custom-close" data-bs-dismiss="modal" aria-label="Close"></button>

                <div class="custom-form-container p-5">
                    <h2 class="text-center fw-bold mb-4">✨ Start Your Journey With Us</h2>

                    <div class="form-floating mb-3">
                        <input type="text" class="form-control custom-input" id="name" placeholder="Name">
                        <label for="name">Name</label>
                    </div>

                    <div class="form-floating mb-3">
                        <input type="email" class="form-control custom-input" id="email" placeholder="Email">
                        <label for="email">Email</label>
                    </div>

                    <div class="form-floating mb-3">
                        <input type="tel" class="form-control custom-input" id="phone" placeholder="Phone">
                        <label for="phone">Phone</label>
                    </div>

                    <div class="form-floating mb-3">
                        <input type="number" class="form-control custom-input" id="age" placeholder="Age">
                        <label for="age">Age</label>
                    </div>

                    <div class="form-floating mb-3">
                        <input type="text" class="form-control custom-input" id="location" placeholder="Where are you from">
                        <label for="location">Where are you from</label>
                    </div>

                    <div class="form-floating mb-4">
                        <input type="text" class="form-control custom-input" id="member" placeholder="Already Member?">
                        <label for="member">Already Member?</label>
                    </div>

                    <div class="text-center">
                        <button type="submit" class="btn btn-lg submit-btn" data-bs-dismiss="modal">Submit</button>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
<style>
    /* === Modal Styles === */
    .custom-modal {
        border-radius: 15px;
        overflow: hidden;
        border: none;
        box-shadow: 0 12px 40px rgba(0, 0, 0, 0.2);
    }

    .custom-modal-body {
        background: linear-gradient(135deg, #667eea, #764ba2); /* Bold Blue-Violet Gradient */
        position: relative;
    }

    /* === Form Container (Glass Effect) === */
    .custom-form-container {
        background-color: rgba(255, 255, 255, 0.18);
        backdrop-filter: blur(16px);
        border-radius: 16px;
        color: #fff;
        padding: 20px;
    }

    /* === Inputs === */
    .custom-input {
        background-color: transparent;
        border: 1px solid rgba(255, 255, 255, 0.3);
        border-radius: 10px;
        color: #fff;
    }

        .custom-input::placeholder {
            color: rgba(255, 255, 255, 0.6);
        }

        .custom-input:focus {
            background-color: transparent;
            color: #fff;
            border-color: #fff;
            box-shadow: none;
        }

    .custom-form-container label {
        color: #f0f0f0;
    }
    /* === Submit Button Update === */
    .submit-btn {
        background-color: #22d3ee; /* Vibrant Teal */
        color: #fff;
        border-radius: 10px;
        padding: 10px 30px;
        font-weight: 600;
        border: none;
        transition: 0.3s ease;
    }

        .submit-btn:hover {
            background-color: #14b8a6; /* Slightly darker Teal */
            transform: scale(1.03);
        }

    /* === Custom Close Button Update === */
    .custom-close {
        position: absolute;
        top: 15px;
        right: 20px;
        width: 38px;
        height: 38px;
        background-color: #ef4444; /* Bold Red */
        border: none;
        border-radius: 50%;
        cursor: pointer;
        z-index: 10;
        box-shadow: 0 0 12px rgba(239, 68, 68, 0.6);
        transition: 0.3s ease;
    }

        .custom-close::before,
        .custom-close::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 18px;
            height: 2px;
            background-color: white;
            transform-origin: center;
        }

        .custom-close::before {
            transform: translate(-50%, -50%) rotate(45deg);
        }

        .custom-close::after {
            transform: translate(-50%, -50%) rotate(-45deg);
        }

        .custom-close:hover {
            background-color: #dc2626;
            box-shadow: 0 0 15px rgba(239, 68, 68, 0.8);
        }


</style>